{
  "name": "office-hours-queue",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered office hours queue MVP",
  "scripts": {
    "start": "python -m uvicorn app.main:app --app-dir backend --host 127.0.0.1 --port 8010",
    "dev": "python -m uvicorn app.main:app --reload --app-dir backend --host 127.0.0.1 --port 8010",
    "start:node": "node server.js"
  }
}
